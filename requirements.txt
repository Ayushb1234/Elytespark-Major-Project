# Streamlit Community Cloud ignores runtime.txt / .python-version and currently
# builds on Python 3.14, so every pin below must have a cp314 wheel. Exact "=="
# pins force a source build (scipy then fails on the missing gfortran), which is
# why lower bounds are used instead.
streamlit>=1.61.1
pandas>=2.3.3,<3
numpy>=2.3.2,<3
scipy>=1.16.1,<2
scikit-learn>=1.7.2,<2
joblib>=1.5.1
matplotlib>=3.10.5
seaborn>=0.13.2

# shap, lime, xgboost and imbalanced-learn are only used by the offline
# training/analysis scripts in src/. They are left out so the deployed app stays
# within Community Cloud's build time and memory limits.
